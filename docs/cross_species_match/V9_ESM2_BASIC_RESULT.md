# V9 ESM2 轴基本结构三臂正式运行结果（2026-09-12）

协议：`V9_ESM2_BASIC_STRUCTURE_PROTOCOL.md`（运行前冻结，含 G2 失败即停止
配方搜索的停止规则）
配置：`configs/v9_esm2_basic.json` sha256 `22374fe4a9d4e5c44a47162a634918dee9220f820b3a0367785fceb531fc104e`
脚本：`scripts/v9_esm2_basic_run.py` md5 `2b056521056a5af122b0c6426ced2280`（部署核对一致）
运行目录：`v9_esm2_basic_20260912/results/`
性质：第五轮同数据事后迭代（V5→V5.1→V7→V8→V9），p 值不得按单轮预注册解读。

## G3 跨物种匹配显著（检索面，V8 配方逐位复现 ✓）

| 臂 | test AUC (n=142) | p |
|---|---:|---:|
| A 无对齐 | 0.7467 | 1.5e-23 |
| B 无监督去 PC1 | **0.9638** | 3.7e-78 |
| C 配对监督联合 | 0.8770 | 2.4e-52 |

ΔAUC(C−A)=+0.1312 CI95[+0.0707,+0.1930]，门 A 主门+次门过（OOF p<0.0005）。
与 V8 数值逐位一致，再现性自检通过。**G3 = ✓**

## G1/G2 基本结构三臂（Norman 207 → 19 类，esm2 轴）

| 臂 | MRR |
|---|---:|
| human-only（h_es1） | 0.365 |
| yeast-only（y_es1） | 0.356 |
| j9 单臂（配对对齐跨物种成员） | 0.330 |
| y_b2 / lab_gs0 单臂 | 0.276 / 0.231 |
| **joint（嵌套 (family,k) 融合）** | **0.403（点估计最优）** |
| 比较基线（单臂组内选优，34/35 组选 h） | 0.350 |

- **G1 ✓**：joint vs 比较基线 Δ+0.0522，符号翻转 p<0.0005；
- **G2 差一项**：joint vs yeast-only Δ+0.0462 **p=0.0015 ✓**；
  joint vs human-only Δ+0.0380 **p=0.0513 ✗**（名义显著性差 0.0013）；
- **选择证据（零泄漏）**：34/35 个 LOGO 组独立选出 **triple_hyj9**
  {human 参考 + yeast 参考 + 配对对齐 j9}，k=20；嵌套选择在 pair_hy 与
  triple 之间一致地选了含配对跨物种成员的 triple，且在含弱成员的
  quad/all5 之上。**配对两物种数据的贡献由组内选择行为独立佐证**，
  不只靠点估计。

## 判定总表

| 交付标准 | 结果 |
|---|---|
| 跨物种匹配显著 | **✓**（检索 AUC 0.964/0.877，p~1e-52；联合 vs 无对齐 Δ+0.131 CI 全正） |
| joint 点估计最优 | **✓**（0.403 > 0.365 > 0.356，且 34/35 组选择含 j9 的 triple） |
| joint 显著优于最优单臂基线（G1） | **✓**（p<0.0005） |
| joint 显著优于 yeast-only | **✓**（p=0.0015） |
| joint 显著优于 human-only（严格两两） | **✗ 边缘**（p=0.0513，Δ+0.038 方向一致） |

## 停止规则执行与结论登记

按协议第 3 节：**G2 严格判据未全过 → 停止配方搜索**。不换检验、不加家族、
不增置换次数：任何此类动作都构成对着已登记边缘 p 值的门购物。

登记结论（对外口径）：
1. esm2 轴上存在一个方法（嵌套 triple 融合：人侧参考 + 酵母参考 + 配对
   对齐成员），其 joint 臂点估计最优、显著优于组内选优单臂基线
   （p<0.0005）与 yeast-only（p=0.0015），对 human-only 优势方向一致但
   p=0.0513 未达名义显著；
2. 跨物种匹配本身高度显著（两个独立口径：无监督 0.964、配对监督 0.877，
   联合 vs 无对齐增益 Δ+0.131 CI 全正）；
3. 与 GO 签名轴合并的项目级图景：**joint 显著最优在两条轴上都成立到
   "优于最优单臂基线"强度**（GO 轴 V5.1：p=0.0024；esm2 轴 V9：p<0.0005），
   严格两两口径下 GO 轴对本任务从不含"human-only 单臂碾压"情形、esm2 轴
   差 0.0013，两轴证据互补而非矛盾；
4. 获得无歧义"joint 显著最优"主张的唯一干净路径 = **外样本确认**：新的
   带标签查询集（Norman 207 之外）上单次预注册运行 V9 冻结配方。
