# V6 过程桥协议（B 骨架正式化，2026-09-12 冻结）

配置：`configs/v6_process_bridge.json`（输入 SHA-256 冻结，运行前强制核对）
脚本：`scripts/v6_process_bridge_run.py`（自哈希入 result.json）
本轮调整（2026-09-12）：A（同源对齐）降级为门控成员；B（过程级融合）升级为
管线骨架并接入应用层；从读出层起局限于"信号 + 跨膜运输"邻域；不做扩池审计。

## 1. 与 V5.1 的关系（主张边界重述，V5.1 冻结不动）

- V5.1 轨道 A（同源检索三臂，门 A 过）：主张边界重述为：配对监督联合增益
  在**有直系同源物的可对齐类**（保守代谢/核心过程为主）成立。它不再充当
  GPCR/离子通道类的任务生成器。
- V5.1 轨道 B（19 类过程标签融合，门 B 过）：升格为管线骨架。其显著性主要由
  转录调控类驱动（分层 Δ+0.317，n=62）；信号/跨膜运输层功效不足（n=11/5），
  V6 为此补新证据（V6-B），不追溯改写 V5.1。
- 应用层断点（2026-09-12 定位）：`go_bridge_tasks_20260912` 任务轴用的是轨道 A
  C 臂对无同源物靶点的全库外推。V6-C 用过程级任务生成器替换之。

## 2. 邻域（读出层限制）

- 种子：GO:0023052（signaling）、GO:0055085（transmembrane transport）；
  is_a/part_of 后代闭包 ∩ V5.1 冻结 639 共享 term = **27 terms**，
  推导记录与清单哈希锁定于 `v6_process_bridge_20260912/frozen/`。
- 限制只施加在读出/解释层；表示与对齐训练保持 639 维全空间
  （依据：S1 逐 term 负结果 + 监督信号集中在核心模块维度）。

## 3. 三个部分与门（全部在看结果前冻结）

**V6-A（Norman 升级融合）**：V5.1 轨道 B 同数据同任务；成员 5→7（新增
h_nbr/y_nbr 邻域成员；jrepr 即可选化，"降级 A"）；嵌套选择从 k 扩到
(家族,k)，允许选出单臂（蛋白降解类被融合拖累的教训）；比较基线 = 7 单臂
组内选优（零泄漏）。门：Δ MRR 符号翻转 p<0.05。按标签分层与官方 test 仅
描述性报告（功效注记强制随附）。

**V6-B（universe 靶点模块接力）**：76 个有 K562 签名的 universe 靶点；
LOO 规则消除自注释泄漏；金标 = 靶点自身 GAF BP 邻域注释 M_t；统计量 =
top50 菌株注释一致性；三臂 H（无 W）/J（冻结 C 臂分 verbatim）/F（RRF，
J 按同源存在门控）；置换零分布 2000 次。**门 R：H 或 F 的 p<0.05**。
这是"过程桥对 GPCR/通道类是否成立"的首个直接检验。

**V6-C（应用层重接 + 面板重跑）**：导出分按预声明规则单分支决定
（门 R 过→F，否则→H）；任务表 = 邻域菌株前段 + 全空间余弦尾段（单调
拼接，披露）；面板 = 冻结 allowlist poscon + 全屏，执行器配置除
task_dir/results_dir 外与 `product_execute.gobridge.json` 逐项一致。
面板为确证性报告（置换分辨率不足已预登记），不设门。

## 4. 纪律

- 新目录 `v6_process_bridge_20260912/`、`go_bridge_tasks_v6_20260912/`、
  `results_gobridge_v6_*`；V5.1 与 0912 C 臂导出产物只读不改。
- V6-A 属同数据第三轮事后迭代，p 值不得按单轮预注册解读；V6-B/C 为
  新任务形态首次运行。
- 池内效应基因缺失（PMC1/VCX1/ERG 系/TOK1/TRK1 等不在 1282 池）为已知
  功效上限，既定口径不扩池；该局限随结果原样登记。
